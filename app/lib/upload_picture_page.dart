import 'package:flutter/material.dart';

class UploadPicturePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Upload Picture'),
      ),
      body: Center(
        child: Text('Upload a picture here'),
      ),
    );
  }
}
